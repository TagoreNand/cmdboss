# =============================================================================
# CMDBoss — Development & Test Dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================

# ---- Test framework ----
pytest==8.1.1
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# ---- Test doubles & clients ----
httpx==0.27.0               # ASGI transport for in-process API tests
mongomock-motor==0.0.29     # async MongoDB mock — tests need no live database
asgi-lifespan==2.1.0        # drive app startup/shutdown in tests

# ---- Linting ----
ruff==0.3.5
