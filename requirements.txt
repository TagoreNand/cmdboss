# =============================================================================
# CMDBoss — Runtime Python Dependencies
# Pin versions for reproducible builds.
# =============================================================================

# ---- Web framework ----
fastapi==0.110.0
uvicorn[standard]==0.29.0
gunicorn==21.2.0

# ---- Database (async MongoDB) ----
motor==3.3.2
pymongo==4.6.2

# ---- Data validation & settings ----
pydantic==2.6.4
pydantic-settings==2.2.1

# ---- Auth ----
pyjwt[crypto]==2.8.0         # JWT/OIDC bearer-token validation (HS256/RS256)

# ---- Observability ----
python-json-logger==2.0.7   # structured JSON logs
prometheus-client==0.20.0   # optional /metrics exposition (degrades to no-op if absent)

# ---- Config loading ----
python-dotenv==1.0.1        # optional .env support for local dev
